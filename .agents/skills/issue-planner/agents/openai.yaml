interface:
  display_name: "Issue Planner"
  short_description: "Propose controller-safe issue plan JSON"
  default_prompt: "Use $issue-planner to return only the issue planning JSON requested by the Controller."
