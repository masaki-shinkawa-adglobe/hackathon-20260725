interface:
  display_name: "Issue Reviewer"
  short_description: "Return read-only Controller review JSON"
  default_prompt: "Use $issue-reviewer to perform the assigned read-only review and return its marker JSON."
