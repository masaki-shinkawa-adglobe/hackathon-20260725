interface:
  display_name: "Issue Reviewer"
  short_description: "Review an issue pull request and merge when ready"
  default_prompt: "Use $issue-reviewer to review the provided Issue and PR, then merge only when ready."
