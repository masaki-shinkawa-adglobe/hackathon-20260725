interface:
  display_name: "Issue Implementer"
  short_description: "Implement and test one Controller worktree"
  default_prompt: "Use $issue-implementer to implement the assigned Issue and return the Controller result JSON."
